# This file is used for configuring the test environment
# It is loaded by the Jest setup file

# Use a separate SQLite database for tests to avoid conflicts with development data
DATABASE_URL="file:./test.db"
